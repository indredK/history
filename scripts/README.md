# 脚本使用说明 / Scripts Usage Guide

## 交互式脚本选择器 (方向键导航) / Interactive Script Selector (Arrow Key Navigation)

### 使用方法 / Usage

你可以通过以下任一方式启动交互式脚本选择器：

```bash
# 方式 1: 直接运行脚本
./scripts/start.sh

# 方式 2: 通过 npm/bun 运行
bun run start
# 或
npm run start
```

### 操作方式 / Controls

- **↑ / ↓** - 上下移动选择
- **Enter** - 确认执行选中的脚本
- **q** - 退出程序

### 功能特性 / Features

- 🎯 **方向键导航** - 使用上下方向键选择脚本，无需输入数字
- 🎨 **彩色输出** - 使用不同颜色区分不同类型的脚本
- 🌐 **双语支持** - 中英文对照显示
- ▶️ **高亮选择** - 当前选中项用箭头和高亮显示
- 🔄 **循环选择** - 到达列表底部时自动回到顶部
- 🚪 **优雅退出** - 支持 q 键快速退出
- 📱 **响应式界面** - 清屏显示，界面简洁

### 可用脚本分类 / Available Script Categories

#### 开发相关 / Development
- `dev:full` - 启动完整开发环境 (前端+后端)
- `dev:frontend` - 仅启动前端开发服务器
- `dev:backend` - 仅启动后端开发服务器

#### 构建相关 / Build
- `build` - 构建整个项目
- `build:frontend` - 仅构建前端
- `build:backend` - 仅构建后端

#### 生产环境 / Production
- `start:backend` - 启动生产环境后端
- `preview` - 预览前端构建结果

#### 代码质量 / Code Quality
- `lint` - 检查代码规范
- `lint:fix` - 自动修复代码规范问题
- `type-check` - TypeScript 类型检查

#### 数据库 / Database
- `db:seed` - 填充数据库种子数据
- `db:reset` - 重置数据库并填充数据

#### 维护相关 / Maintenance
- `clean` - 清理所有依赖和构建文件
- `clean:install` - 清理并重新安装所有依赖
- `install:all` - 安装所有依赖

#### 其他脚本 / Other Scripts
- `verify-deploy` - 验证部署状态

### 使用示例 / Usage Examples

```bash
# 启动交互式选择器
./scripts/start.sh

# 使用方向键导航
# ↑ - 向上移动
# ↓ - 向下移动
# Enter - 执行选中的脚本
# q - 退出

# 界面示例:
#   ▶ 🚀 启动完整开发环境 (前端+后端) / Start Full Dev Environment ◀
#     🎨 仅启动前端开发服务器 / Frontend Dev Server Only
#     📦 仅启动后端开发服务器 / Backend Dev Server Only
#     ...
```

### 直接运行特定脚本 / Direct Script Execution

如果你知道要运行的具体脚本，也可以直接使用：

```bash
# 开发环境
bun run dev              # 完整开发环境
bun run dev:frontend     # 仅前端
bun run dev:backend      # 仅后端

# 构建
bun run build            # 构建全部
bun run build:frontend   # 仅构建前端
bun run build:backend    # 仅构建后端

# 生产环境
bun run start:prod       # 生产环境后端
bun run preview          # 预览前端

# 代码质量
bun run lint             # 代码检查
bun run lint:fix         # 自动修复
bun run type-check       # 类型检查

# 数据库
bun run db:seed          # 填充数据
bun run db:reset         # 重置数据库

# 维护
bun run clean            # 清理
bun run clean:install    # 清理并重装
bun run install:all      # 安装依赖
```

## 脚本文件说明 / Script Files

- `start.sh` - 交互式脚本选择器主文件
- `dev.sh` - 开发环境启动脚本
- `verify-deploy.sh` - 部署验证脚本

## 自定义脚本 / Custom Scripts

如需添加新的脚本选项，请编辑 `scripts/start.sh` 文件中的 `show_scripts()` 和 `execute_script()` 函数。