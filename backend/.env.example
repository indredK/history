# Database — 本地默认 SQLite,文件位于 backend/prisma/dev.db
# 启动前请运行: bunx prisma migrate deploy && bunx prisma db seed
DATABASE_URL="file:./prisma/dev.db"

# Server
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# API
API_PREFIX=/api/v1
CORS_ORIGIN=http://localhost:5173
