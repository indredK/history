# 安全政策

## 支持版本

当前项目尚未发布稳定版本。`main` 分支和最新未发布版本为主要维护对象；历史开发分支不承诺安全修复。

| 版本 | 支持状态 |
|---|---|
| `main` / Unreleased | 支持 |
| 其它分支或个人 fork | 不支持 |

## 漏洞报告

请不要在公开 Issue 中披露安全漏洞。当前私密报告渠道仍在建设中，临时流程如下:

1. 通过维护者私信或项目约定的私密渠道发送报告。
2. 报告中请包含影响范围、复现步骤、相关日志或请求样例、建议修复方式。
3. 维护者确认后会在私密渠道同步修复计划，并在修复发布后公开摘要。

目标响应时间:

| 阶段 | 目标 |
|---|---|
| 首次确认 | 3 个工作日内 |
| 初步影响评估 | 7 个工作日内 |
| 高危问题修复 | 14 个工作日内，视影响范围调整 |

## 当前安全状态

已具备:

- Swagger 中预留 `JWT-auth` Bearer 认证方案。
- 全局异常过滤器，避免生产环境默认暴露异常堆栈。
- 全局响应拦截器，统一 API 响应结构。
- 基础请求日志拦截器。

待实现:

- [ ] 接入 `helmet`，生产环境启用安全响应头和 CSP 策略。
- [ ] 接入 `@nestjs/throttler` 或等价方案，对全局 API 和写接口加速率限制。
- [ ] 实现 JWT 认证、AuthGuard、公开路由白名单和角色/权限策略。
- [ ] 将日志升级为结构化日志，例如 pino，包含 request id、status、duration、user id。
- [ ] 收紧生产 CORS origin，文档化 HTTPS 反向代理、安全 cookie 和环境变量管理。
- [ ] 为写接口补齐输入长度、数组长度、JSON 字段大小和枚举白名单校验。
- [ ] 建立依赖漏洞扫描和安全修复发布流程。

## 数据与隐私

- 不要提交 API key、令牌、数据库凭据或任何个人敏感信息。
- 历史数据贡献必须附来源与许可；未经许可的商业数据禁止提交。
- 本地 SQLite 数据库仅用于开发，生产环境需要独立的备份、访问控制和迁移策略。
